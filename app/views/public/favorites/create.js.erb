$('#post-<%= @post.id %> .favorite-link').replaceWith('<%= j(link_to "♥", post_favorite_path(@post, current_user.favorites.find_by(post: @post)), method: :delete, remote: true, class: 'btn btn-sm btn-outline-danger favorite-link') %>');
<!-- 非同期通信 --